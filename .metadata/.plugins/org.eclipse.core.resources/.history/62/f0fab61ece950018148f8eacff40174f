package com.model.test

import com.test.TestDomain
import org.junit.Before
import tools.vitruv.framework.tuid.TuidManager
import com.test.TestDomainProvider
import com.test.test.TestFactory
import org.junit.Test
import org.junit.Assert

class TestDomainTest {
	private static val TEST_NAME = "Test";
	private var TestDomain testDomain;
	
	@Before
	public def void setup() {
		//TuidManager.instance.reinitialize();
		//testDomain = new TestDomainProvider().domain;
	}
	
	@Test
	public def void testResponsibilityChecks() {
		//val clazz = TestFactory.eINSTANCE.createStimulusEvent();
		//Assert.assertTrue(asemDomain.isInstanceOfDomainMetamodel(clazz));
		//Assert.assertTrue(asemDomain.calculateTuid(clazz) !== null);
		
		Assert.assertTrue(true,true);
	}
	
}